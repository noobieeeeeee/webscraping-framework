name: price-extractor
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - playwright>=1.52.0
    # Optional agentic dependencies (uncomment to enable):
    # - langgraph>=0.2.0
    # - langchain-core>=0.3.0
    # Optional dev dependencies:
    # - pytest>=7.0
    # - pytest-cov>=4.0
    # - black>=23.0
    # - ruff>=0.1.0
